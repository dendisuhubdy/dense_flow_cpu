cmake_minimum_required(VERSION 2.8)
project( denseFlow )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( denseFlow_cpu denseFlow_cpu.cpp )
target_link_libraries( denseFlow_cpu ${OpenCV_LIBS} )
